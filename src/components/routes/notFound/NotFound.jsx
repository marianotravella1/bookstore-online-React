import React from "react";

const NotFound = () => {
  const handleGoBack = () => {
    naviga
  };

  return (
    <div>
      <h2>404 not found</h2>
      <button onClick={handleGoBack}>Go back</button>
    </div>
  );
};

export default NotFound;
